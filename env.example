# Vercel Edge Config (writes require API)
EDGE_CONFIG_ID=ecfg_xxx
EDGE_CONFIG_TOKEN=vercel_token_xxx
# Optional: only if the Edge Config is under a team
# EDGE_CONFIG_TEAM_ID=team_xxx

# API Configuration (Vite)
VITE_API_URL=/api

# Vercel Blob (optional, used for uploads if enabled)
# BLOB_READ_WRITE_TOKEN=your_vercel_blob_token_here

# Environment
NODE_ENV=development
